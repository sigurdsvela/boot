#!/usr/bin/env ruby

args = ARGV.join(' ')

currentDir = File.expand_path(File.dirname(__FILE__))
libDir = File.dirname(currentDir) + '/lib'

# Run the main boot file with desired include path
exec("ruby -I#{libDir} #{currentDir}/boot.rb #{args}")
